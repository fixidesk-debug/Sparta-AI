[pytest]
minversion = 6.0
addopts = -ra -q --cov=app --cov-report=term-missing --cov-report=html:htmlcov --cov-fail-under=80
testpaths = tests
python_files = test_*.py

markers =
    integration: marks tests that require external services (postgres, redis)
    slow: marks slow tests
